---
import { Image } from "astro:assets"

const { id, title, image } = Astro.props
---

<Image
  src={image || `/archive/ogp/${id}.webp`}
  alt={title}
  width="1200"
  height="630"
  class="rounded-t-lg object-cover"
/>
